// Function to set the "user" cookie to "admin"
function setAdminCookie() {
    document.cookie = "user=admin";
}

// Check if the user has the "user" cookie with value "admin"
if (document.cookie.includes("user=admin")) {
    // Replace the flag content below with your actual flag
    document.write("<p>Flag: adm1n_c00ki3_537584</p>");
}

